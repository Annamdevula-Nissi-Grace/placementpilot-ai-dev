import {useState} from 'react';import {Card,Icon,SectionTitle} from '../components/UI';
export default function Coach(){const [messages,setMessages]=useState<string[]>(['AI: Hi Nissi! I can help you plan your placement preparation.']);const [text,setText]=useState('');const send=(t=text)=>{if(!t.trim())return;setMessages(x=>[...x,`You: ${t}`,`AI: Based on your current readiness, spend 45 minutes on DSA and 20 minutes on interview communication today.`]);setText('')};return <><div className="page-head"><div><h1>Career Coach</h1><p>Your AI preparation companion.</p></div></div><div className="coach-layout"><Card className="chat-large"><SectionTitle title="AI Career Coach"/><div className="messages">{messages.map((m,i)=><p className={m.startsWith('AI')?'ai-msg':'user-msg'} key={i}>{m}</p>)}</div><div className="suggestions">{['Build my study plan','How do I prepare for Infosys?','What should I learn next?'].map(x=><button className="btn" onClick={()=>send(x)} key={x}>{x}</button>)}</div><div className="chat-input"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Ask your career coach..."/><button onClick={()=>send()}><Icon>send</Icon></button></div></Card><div className="stack"><Card><SectionTitle title="Today's Focus"/><p>1. Arrays & Strings</p><p>2. SQL joins</p><p>3. STAR interview answers</p></Card><Card className="ai-card"><SectionTitle title="Readiness Snapshot"/><strong className="metric">72%</strong><p>You're on track. Consistency is the biggest lever now.</p></Card></div></div></>}
